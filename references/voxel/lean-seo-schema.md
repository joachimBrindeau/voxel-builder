# lean-seo schema — JSON-LD structured data, Voxel-relation aware

The `schema` module (`modules/schema/`) emits JSON-LD structured data per CPT. Config persists **one option per target** — `lean_seo_schema:{target}` (JSON string) plus a `lean_seo_schema_targets` index option; a legacy monolithic `lean_seo_schema` option is auto-migrated to per-target on `plugins_loaded` (prio 5) then deleted. This is the richest Voxel surface: its source grammar has dedicated resolvers for Voxel fields, relations, relation-fields, and hierarchy. Read [`lean-seo-settings-substrate.md`](lean-seo-settings-substrate.md) first.

## Bounded Enrichment Rules

- `post:thumbnail_object` resolves an `ImageObject`, using the configured thumbnail size and a
  safe fallback when attachment metadata is incomplete. Prefer it to assembling image properties
  from unrelated scalar sources; change the size through its current filter rather than forking
  the resolver.
- Aggregate sources use the registered `aggregate:` grammar. Validate the source key and emitted
  numeric type against the current registry before adding `AggregateRating`; omit the node when
  no real rating/count exists rather than publishing invented defaults.
- Relation/query-backed sources are bounded per key by `lean_seo_schema_query_limit`. Never use
  `-1` or an unbounded relation scan in render-time schema. When a result is truncated, the
  plugin's truncation action is the observability hook; tests must cover limit, omission, and
  truncation behavior.
- Verify the final JSON-LD on a populated and sparse record. Passing config validation does not
  prove that runtime sources resolve, that fallbacks prune cleanly, or that query limits hold.

## Config model — per-target `@type` + property→source map

Schema config is keyed by **target**, retrieved via `lean_seo_schema_get_configs()` (two-layer cached: static memo + persistent `schema_configs`, flushed on save/delete). Assignable targets (`lean_seo_schema_get_assignable_targets`): public/queryable post types → key `<post_type>`; public taxonomies → key `taxonomy:<slug>`. Render-time archive targets: `front_page`, `archive:<post_type>`, `taxonomy:<slug>`, `author` (author archives route to the `profile` CPT config — see below).

CRUD: `lean_seo_schema_save_config($target,$config)`, `lean_seo_schema_delete_config($target)`. A stored value counts as a config only if it has `@type` or `@graph` (`lean_seo_schema_is_config`).

Each entry is a JSON-LD skeleton whose leaf values are **source strings** resolved at render time. Reserved keys (`LEAN_SEO_SCHEMA_RESERVED_KEYS`): `@type, @id, @graph, @each, @ref, @filter, @list, @require` (plus `@map` inside `@each` templates). Shape:

```json
{
  "@type": "ProfessionalService",
  "@id":   "@ref:self#professionalservice",
  "@require": "voxel:business_name",
  "name":  "voxel:business_name|decode",
  "telephone": "voxel:phone|phone",
  "url":   "post:permalink",
  "address": { "@type": "PostalAddress", "streetAddress": "voxel:location.address", "addressLocality": "meta:city" },
  "sameAs": { "@each": "voxel:social_links", "@map": "row:url" },
  "areaServed": { "@each": "relation:service_areas", "@type": "Place", "name": "related:title" }
}
```

A config may be a single node (`@type` + properties) or a `@graph` array of nodes. `lean_seo_schema_type_fragment()` derives the `@id` fragment from `@type`. The vocabulary (`vocabulary.php`, generated by `wpdev schema:compile` from schema.org) is the type/property authority; `lean_seo_schema_validate` enforces `@type` exists and each property is valid for it.

### Control keys (renderer, `render.php`)

- **`@each: <list-source>`** — iterate a resolved list (post IDs or repeater rows); remaining keys form a per-item template. Integer items → `related_id` context (public-filtered); array items → `row` context (with `row.index`/`row.position`).
- **`@map: "<source>"`** — inside `@each`, resolve one source per row to a scalar → flat de-duplicated array (e.g. `sameAs`).
- **`@ref: true|"<fragment>"`** — inside `@each`, each item → `{"@id": "permalink#fragment"}`.
- **`@filter: "field=value"`** — skip non-matching repeater rows.
- **`@require: <source|list>`** — node is skipped unless **all** listed sources resolve non-empty (gates optional nodes without PHP).
- **`@list: [<source>,...]`** — resolve each to a flat, empties-dropped array.
- Object pruning: a node with 0 properties, or only static `@value:` literals and no dynamic child, is dropped.

## Source grammar — `prefix:key|transform|transform`

`modules/schema/sources.php` → `lean_seo_schema_resolve_source()` parses `prefix:key|t1|t2` (`lean_seo_schema_parse_source()`) and dispatches on prefix. Registry (`lean_seo_schema_source_registry()`, filter `lean_seo_schema_sources`):

| Prefix | Resolves | File |
|---|---|---|
| `post:` | Core post field (title, permalink, excerpt, date…); `post:children` = published child IDs | sources/post.php |
| `meta:` | Raw post meta key (auto-JSON-decoded) | sources/post.php |
| `voxel:` | **Voxel field via `lean_seo_voxel_field_value()`** — handles repeaters/relations/locations; supports dot-path (`voxel:schedule.0.start`) | sources/related.php |
| `relation:` | **Voxel relation → array of published post IDs** (for `@each`) via `lean_seo_get_related_ids()` | sources/related.php |
| `relation_field:rel.field` | A field from the **first related** post through relation `rel` | sources/related.php |
| `related:` | Field from the current `@each` related post (`related:title`, `related:meta:key`) | sources/related.php |
| `hierarchy:` | Voxel/WP parent-child ancestry (breadcrumb-like) | sources/hierarchy.php |
| `seo:` | A resolved lean-seo value (`title/desc/description/image` from `$ctx['seo']`) | sources/post.php |
| `author:` | Post author `name/url/email` | sources/post.php |
| `tax:` / `term:` | Taxonomy term names / term-archive fields (`name/description/link/posts`) | sources/post.php |
| `parent:` | Delegates `post:` against the WP core parent (public only) | sources/post.php |
| `row:` | Current repeater row field (inside a repeater `@each`), dot-path | sources/post.php |
| `var:` | lean-seo site variable (`lean_seo_var`); `tel`-typed vars phone-formatted | sources/post.php |
| `option:` | **Allow-listed** wp_options only (`blogname, blogdescription, siteurl, home, timezone_string, date_format, time_format, WPLANG`; filter `lean_seo_schema_allowed_options`) | sources/post.php |
| `aggregate:` | `aggregate:post_type.meta_key.stat` — `avg`/`count`/`best`/`worst` over published posts, SQL, 1h cached | sources/aggregate.php |
| `query:` | **Site-specific** published-ID lists — only `query:org_ids` / `query:glossaire_ids` exist; other keys return null | sources/query.php |
| `@value:` | Static literal (`@value:5\|int`); statics don't count as "dynamic" for object pruning | sources.php (special) |
| `@ref:` | Entity reference → `{"@id": "..."}` (`self#frag`, `site#frag`, `relation:field`, `hierarchy:scope`, `parent:type`) | sources/ref.php |
| `concat:` | Join resolved sources; segment[1] is the reused separator | sources.php (special) |

### `@each` loops over Voxel relations

The relation→`@each`→`related:` triad is how a Voxel post's related items become a JSON-LD array (services, locations, reviews). `relation:services` yields the published IDs; the `@each` block renders once per ID with `related:*` reading from that related post. `relation_field:location.geo-region` is the single-related shorthand (first related post's `geo-region` field).

### Voxel field resolution detail

`voxel:` → `lean_seo_schema_resolve_voxel()` calls `lean_seo_voxel_field_value($post_id, $field_key)`, which uses Voxel's `get_field()->get_value_from_post()`. Dot-paths (`voxel:location.address`) then walk the returned structure via `lean_seo_schema_resolve_path()`. On non-Voxel sites it falls back to `meta:`. Relation resolvers filter to `publish` status (`lean_seo_schema_is_public()`).

## Transforms

`modules/schema/transforms.php` applies `|`-piped transforms after resolution (`lean_seo_schema_apply_transforms`; null short-circuits). Registry (filter `lean_seo_schema_transforms`): `phone`, `bool`, `float`, `int`, `email`, `date` (→ ISO-8601 `gmdate('c')`), `decode` (`html_entity_decode`), `url_encode`, `strip_tags`, `schema_url` (bare token → `https://schema.org/<token>`, absolute URLs pass through — portable enums like `InStock`), `days` (short day codes → `DayOfWeek` names), plus **site-specific** `contract_type` (FR contract → `employmentType`) and `job_location_type`. Pipe multiple: `voxel:phone|phone`, `post:date|date`. No generic URL-absolutization transform — absolute URLs come from `permalink`/`var` sources.

## Hierarchy source detail

`hierarchy:scope[.field]` (`sources/hierarchy.php`). Scopes: `parent, root, ancestors, trail`/`breadcrumb` (ancestors+self), `children, siblings, depth`, `position`. Adding `.field` maps that field over the resolved ID(s). Data source: voxel-addon `\VoxelAddon\Modules\Hierarchy\Engine\HierarchyMaintainer` (`ancestor_ids`/`child_ids`/`sibling_ids`) when present, else Voxel relation fields named `hierarchy-{post_type}-ancestors|children|siblings`; `parent`/`root` derive from WP core `post_parent`. All IDs public-filtered.

## Validation + vocabulary

- `modules/schema/validate.php` — validates the resolved graph (required properties per type, source-string well-formedness; branches on the special-prefix vocabulary `LEAN_SEO_SCHEMA_SPECIAL_PREFIXES`).
- `modules/schema/vocabulary.php` — the allowed schema.org types/props the builder + validator constrain against.
- `modules/schema/render.php` — assembles and prints the `<script type="application/ld+json">`.
- Tests live in `tests/Unit/Schema/` (Source, QuerySource, Hierarchy, Breadcrumb, Validate, NoHardcodedFallbacks).

## Author/profile + breadcrumb specifics

- **Author archives** → the `profile` CPT config renders against the linked profile post (substrate §author↔profile; filter `lean_seo_schema_author_context`).
- **BreadcrumbList** (`modules/schema/breadcrumb.php`) is emitted from the parent-derived permalink hierarchy — same ancestry `hierarchy:` reads.

## Configure

Admin: **Schemas** page (`lean_seo_schema_page` list + `lean_seo_schema_page_new` editor, `?target=<key>`) — type-cascade + property list + raw-JSON textarea + live Preview. AJAX (`settings/ajax.php`, guarded by `lean_seo_schema_nonce`): `save`, `delete`, `export`, `import`, `preview`, `posts`; every inbound config is `json_decode`d then recursively `sanitize_text_field`d before validation. CLI/inspection — config is **per target**:

```bash
wp option get lean_seo_schema_targets          # the index of configured targets
wp option get "lean_seo_schema:org"            # one CPT's config (JSON string)
wp option update "lean_seo_schema:org" '<json>'
```

Do NOT write a monolithic `lean_seo_schema` option — it's the legacy shape, auto-migrated to per-target then deleted.

## Gotchas

- **`voxel:` vs `meta:`** — always prefer `voxel:` for Voxel fields; `meta:` reads raw storage which for relations/locations/repeaters is not the rendered value.
- **`relation:` needs `@each`** — a bare `relation:services` yields an ID array; assign it to a property only through an `@each` block, else you emit raw IDs.
- **Public-status filter** — related/relation sources drop non-published posts silently; a draft related item simply won't appear.
- **Non-Voxel fallback** — on a non-Voxel site `voxel:`/`relation:` degrade to `meta:`/JSON-array; don't assume Voxel semantics when writing a portable config.
- **Dot-paths** are for structured field values only (`lean_seo_schema_resolve_path`), returning null on a missing path — a typo silently drops the property.
